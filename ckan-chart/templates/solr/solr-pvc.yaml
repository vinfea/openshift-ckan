apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ckan-solr-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.solr.pvc.storage }}
  # storageClassName can be omitted to use the default StorageClass
  # of the OpenShift sandbox
